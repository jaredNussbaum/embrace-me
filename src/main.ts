import * as THREE from "three";
import "./style.css";

document.body.innerHTML = `
  <h1>EMBRACE ME</h1>
`;

// deno-lint-ignore no-unused-vars
const scene = new THREE.Scene();
// const camera = new THREE.PerspectiveCamera(75, globalThis.innerWidth / globalThis.innerHeight);
